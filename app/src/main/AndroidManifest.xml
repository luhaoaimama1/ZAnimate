<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zone.com.zanimate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="._2DActivity"></activity>
        <activity android:name="._3DActivity"></activity>
        <activity android:name="._ReboundActivity"></activity>
        <activity android:name="._ImitationActivity"></activity>
        <activity android:name="._AniActivity"></activity>
        <activity android:name="._AniActivity2"></activity>
    </application>

</manifest>